command:
  - uvicorn
  - app:app
  - --host
  - 0.0.0.0
  - --port
  - "8000"

env:
  - name: DATABRICKS_WAREHOUSE_ID
    description: "SQL Warehouse ID for running metadata queries"
    value: "REPLACE_WITH_YOUR_WAREHOUSE_ID"
